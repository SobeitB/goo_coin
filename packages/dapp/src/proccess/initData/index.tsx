import {memo, ReactNode, useEffect} from "react";


export const InitData = memo(({children}:{children:ReactNode}) => {

      useEffect(() => {
            return
      }, [])


      return children
})