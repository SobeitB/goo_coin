import {currentChain} from "../../../blockchain";

export const IS_INFO = 'IS_INFO';

export const CHANGE_NETWORK = `Change the network to the ${currentChain.name}!`;
export const BALANCE_UPDATE = 'Your balance was successfully updated!';
